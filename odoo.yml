../odoo:
  remotes:
    origin: https://github.com/odoo/odoo
  target: origin $ODOO_VERSION
  merges:
    - origin $ODOO_VERSION
    # [FIX] oid in serialize
    - origin refs/pull/141831/head

design-themes:
  defaults:
    depth: 1
  remotes:
    origin: https://github.com/odoo/design-themes
  target: origin $ODOO_VERSION
  merges:
    - origin $ODOO_VERSION
