
account-analytic:
  defaults:
    depth: 1
  remotes:
    origin: https://github.com/OCA/account-analytic
  target: origin $ODOO_VERSION
  merges:
    - origin $ODOO_VERSION

agreement:
  defaults:
    depth: 1
  remotes:
    origin: https://github.com/OCA/agreement
  target: origin $ODOO_VERSION
  merges:
    - origin $ODOO_VERSION

account-budgeting:
  remotes:
    origin: https://github.com/OCA/account-budgeting
  target: origin $ODOO_VERSION
  merges:
    - origin $ODOO_VERSION
    - origin refs/pull/55/head # Accont Budget OCA

account-closing:
  defaults:
    depth: 1
  remotes:
    origin: https://github.com/OCA/account-closing
  target: origin $ODOO_VERSION
  merges:
    - origin $ODOO_VERSION
    
account-financial-tools:
  remotes:
    origin: https://github.com/OCA/account-financial-tools
  target: origin $ODOO_VERSION
  merges:
    - origin $ODOO_VERSION
    #- origin refs/pull/1301/head # account_check_deposit

account-reconcile:
  defaults:
    depth: 1
  remotes:
    origin: https://github.com/OCA/account-reconcile
  target: origin $ODOO_VERSION
  merges:
    - origin $ODOO_VERSION
    
bank-statement-import:
  defaults:
    depth: 1
  remotes:
    origin: https://github.com/OCA/bank-statement-import
  target: origin $ODOO_VERSION
  merges:
    - origin $ODOO_VERSION

brand:
  defaults:
    depth: 1
  remotes:
    origin: https://github.com/OCA/brand
  target: origin $ODOO_VERSION
  merges:
    - origin $ODOO_VERSION

connector:
  defaults:
    depth: 1
  remotes:
    origin: https://github.com/OCA/connector
  target: origin $ODOO_VERSION
  merges:
    - origin $ODOO_VERSION

connector-telephony:
  remotes:
    origin: https://github.com/OCA/connector-telephony
  target: origin $ODOO_VERSION
  merges:
    - origin $ODOO_VERSION
    - origin refs/pull/270/head # ADD sms_sendinblue_http

contract:
  defaults:
    depth: 1
  remotes:
    origin: https://github.com/OCA/contract
  target: origin $ODOO_VERSION
  merges:
    - origin $ODOO_VERSION

credit-control :
  defaults:
    depth: 1
  remotes:
    origin: https://github.com/OCA/credit-control
  target: origin $ODOO_VERSION
  merges:
    - origin $ODOO_VERSION

crm:
  defaults:
    depth: 1
  remotes:
    origin: https://github.com/OCA/crm
  target: origin $ODOO_VERSION
  merges:
    - origin $ODOO_VERSION

delivery-carrier:
  remotes:
    origin: https://github.com/OCA/delivery-carrier
    moka: https://github.com/Moka-Tourisme/delivery-carrier
  target: origin $ODOO_VERSION
  merges:
    - origin $ODOO_VERSION
    - moka refs/pull/4/head

e-commerce:
  remotes:
    origin: https://github.com/OCA/e-commerce
    moka: https://github.com/Moka-Tourisme/e-commerce
  target: origin $ODOO_VERSION
  merges:
    - origin $ODOO_VERSION
    - moka $ODOO_VERSION
    #- origin refs/pull/656/head  # website_sale_cart_expire
    
edi:
  defaults:
    depth: 1
  remotes:
    origin: https://github.com/OCA/edi
  target: origin $ODOO_VERSION
  merges:
    - origin $ODOO_VERSION

event:
  remotes:
    origin: https://github.com/OCA/event
    moka: https://github.com/Moka-Tourisme/event
  target: origin $ODOO_VERSION
  merges:
    - origin $ODOO_VERSION
    #- origin refs/pull/257/head  # event_registration_mail_compose
    - origin refs/pull/258/head  # event_registration_sms_compose
    #- origin refs/pull/260/head  # website_event_sale_cart_quantity_readonly
    - origin refs/pull/275/head  # event_session
    - origin refs/pull/276/head  # event_sale_session
    #- moka refs/pull/5/head # Fix mail not sent on event, edit on oca
    - moka 15.0-add-website-event-session
    - moka 15.0-add-website-event-sale-session
    - moka 15.0-add-event-portal
    - moka 15.0-add-event-portal-ticket
    - moka 15.0-fix-event-mail-session

knowledge:
  defaults:
    depth: 1
  remotes:
    origin: https://github.com/OCA/knowledge
  target: origin $ODOO_VERSION
  merges:
    - origin $ODOO_VERSION

l10n-france:
  defaults:
    depth: 1
  remotes:
    origin: https://github.com/OCA/l10n-france
  target: origin $ODOO_VERSION
  merges:
    - origin $ODOO_VERSION

margin-analysis:
  defaults:
    depth: 1
  remotes:
    origin: https://github.com/OCA/margin-analysis
  target: origin $ODOO_VERSION
  merges:
    - origin $ODOO_VERSION

mis-builder:
  defaults:
    depth: 1
  remotes:
    origin: https://github.com/OCA/mis-builder
  target: origin $ODOO_VERSION
  merges:
    - origin $ODOO_VERSION

partner-contact:
  defaults:
    depth: 1
  remotes:
    origin: https://github.com/OCA/partner-contact
  target: origin $ODOO_VERSION
  merges:
    - origin $ODOO_VERSION

pos:
  remotes:
    origin: https://github.com/OCA/pos
    moka: https://github.com/Moka-Tourisme/pos
  target: origin $ODOO_VERSION
  merges:
    - origin $ODOO_VERSION
    # Prevent common merge conflicts in OCA PRs
    # Some PRs will cause conflicts due to multiple changes on files like test-requirements.txt
    # So, before anything else, merge this branch that sets a specific merging strategy.
    - moka 15.0-fix-git-aggregate
    - moka 15.0-fix-pos-event-sale
    - moka 15.0-add-pos-event-sale-express
    - moka 15.0-add-pos-event-sale-ticket-order
    - moka 15.0-pos-restaurant-better-readability
    - moka $ODOO_VERSION
    # Pending OCA PRs:
    #- origin refs/pull/805/head  # pos_sale_pos_event_sale
    - origin refs/pull/808/head  # pos_event_sale_session
    - origin refs/pull/810/head  # pos_sale_pos_event_sale_session

product-pack:
  defaults:
    depth: 1
  remotes:
    origin: https://github.com/OCA/product-pack
  target: origin $ODOO_VERSION
  merges:
    - origin $ODOO_VERSION

project:
  defaults:
    depth: 1
  remotes:
    origin: https://github.com/OCA/project
  target: origin $ODOO_VERSION
  merges:
    - origin $ODOO_VERSION
    
purchase-workflow:
  defaults:
    depth: 1
  remotes:
    origin: https://github.com/OCA/purchase-workflow
  target: origin $ODOO_VERSION
  merges:
    - origin $ODOO_VERSION

queue:
  defaults:
    depth: 1
  remotes:
    origin: https://github.com/OCA/queue
  target: origin $ODOO_VERSION
  merges:
    - origin $ODOO_VERSION

reporting-engine:
  defaults:
    depth: 1
  remotes:
    origin: https://github.com/OCA/reporting-engine
  target: origin $ODOO_VERSION
  merges:
    - origin $ODOO_VERSION

rest-framework:
  defaults:
    depth: 1
  remotes:
    origin: https://github.com/OCA/rest-framework
  target: origin $ODOO_VERSION
  merges:
    - origin $ODOO_VERSION
    
sale-reporting:
  remotes:
    origin: https://github.com/OCA/sale-reporting
  target: origin $ODOO_VERSION
  merges:
    - origin $ODOO_VERSION
    # Pending OCA PRs:
    #- origin refs/pull/163/head  # sale_layout_category_hide_detail    
    
sale-promotion:
  defaults:
    depth: 1
  remotes:
    moka: https://github.com/Moka-Tourisme/sale-promotion
  target: moka $ODOO_VERSION
  merges:
    - moka $ODOO_VERSION

sale-workflow:
  defaults:
    depth: 1
  remotes:
    origin: https://github.com/OCA/sale-workflow
  target: origin $ODOO_VERSION
  merges:
    - origin $ODOO_VERSION

server-auth:
  defaults:
    depth: 1
  remotes:
    origin: https://github.com/OCA/server-auth
  target: origin $ODOO_VERSION
  merges:
    - origin $ODOO_VERSION
    
server-brand:
  defaults:
    depth: 1
  remotes:
    origin: https://github.com/OCA/server-brand
  target: origin $ODOO_VERSION
  merges:
    - origin $ODOO_VERSION

server-tools:
  remotes:
    origin: https://github.com/OCA/server-tools
  target: origin $ODOO_VERSION
  merges:
    - origin $ODOO_VERSION
    #- origin refs/pull/2340/head # MIG iap_alternative_provider

server-ux:
  remotes:
    origin: https://github.com/OCA/server-ux
    moka: https://github.com/Moka-Tourisme/server-ux
  target: origin $ODOO_VERSION
  merges:
    - origin $ODOO_VERSION
    - moka $ODOO_VERSION

social:
  remotes:
    origin: https://github.com/OCA/social
  target: origin $ODOO_VERSION
  merges:
    - origin $ODOO_VERSION
    #- origin refs/pull/933/head  # #1740 fix mail debrand

stock-logistics-barcode:
  defaults:
    depth: 1
  remotes:
    origin: https://github.com/OCA/stock-logistics-barcode
  target: origin $ODOO_VERSION
  merges:
    - origin $ODOO_VERSION

timesheet:
  defaults:
    depth: 1
  remotes:
    origin: https://github.com/OCA/timesheet
  target: origin $ODOO_VERSION
  merges:
    - origin $ODOO_VERSION

web:
  defaults:
    depth: 1
  remotes:
    origin: https://github.com/OCA/web
  target: origin $ODOO_VERSION
  merges:
    - origin $ODOO_VERSION

website:
  remotes:
    origin: https://github.com/OCA/website
    moka: https://github.com/Moka-Tourisme/website
  target: origin $ODOO_VERSION
  merges:
    - origin $ODOO_VERSION
    - moka 15.0-add-membership
